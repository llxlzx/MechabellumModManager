# 钢铁指挥官 Mod 管理器 — 分发 / 使用说明

Mechabellum Mod Manager — distribution & end-user notes

> **说明 / Notice**  
> 本文面向最终用户与分发包维护者，说明安装目录、游戏目录与日常用法。路径与组件选项以安装向导实际页面为准。  
> **This document is for end users and package maintainers. Install-dir / game-dir steps follow the Setup wizard; adapt paths to your machine.**

> **⚠️ AI 生成声明 / AI-generated notice**  
> 中英文说明主要由 AI 辅助整理，可能存在表述偏差；请以安装向导与应用内说明为准。  
> **Chinese/English text was largely AI-assisted; prefer the Setup wizard and in-app notices.**

## 语言 / Language

- 中文：[系统要求](#1-系统要求-中文) · [安装](#2-安装推荐-中文) · [使用](#3-日常使用-中文) · [MelonLoader](#4-melonloader-中文) · [风险](#5-风险提示-中文)
- English: [Requirements](#1-requirements-english) · [Install](#2-install-recommended-english) · [Usage](#3-daily-use-english) · [MelonLoader](#4-melonloader-english) · [Risks](#5-risk-notice-english)

---

## 1. 系统要求 (中文)

- Windows 10 / 11 **64 位**
- Steam 版 *Mechabellum*（钢铁指挥官）
- 建议使用本仓库 **[Releases](https://github.com/llxlzx/MechabellumModManager/releases/latest)** 提供的 Setup 安装包（管理员运行）

---

## 2. 安装（推荐） (中文)

1. 运行 `MechabellumModManager_Setup_v*.exe`（或中文文件名的同内容安装包），**以管理员身份**运行。  
2. **选择管理器安装位置**（程序装到哪里）。  
3. **选择游戏目录**：需包含 `Mechabellum.exe` 与 `GameAssembly.dll`（可自动探测 Steam 路径）。  
4. 组件建议保持默认：
   - **.NET 8 Desktop Runtime** — 运行管理器  
   - **.NET 6 Desktop Runtime** — MelonLoader 常用依赖  
   - **MelonLoader** — 若游戏里还没有 Loader（已安装会跳过）  
5. 完成安装后从开始菜单启动管理器。

若本机已装对应 Runtime，安装程序会自动跳过。

离线环境：制作 Setup 时把官方 Runtime / MelonLoader zip 放进 `installer/redist/`（见该目录说明），再编译安装包。

---

## 3. 日常使用 (中文)

1. **导入 Mod**：`导入 DLL` / `导入 Zip` / `导入文件夹`，或「从游戏导入」。  
2. 在左侧选中一个**方案**，勾选要启用的 Mod。  
3. 点 **应用方案**（只同步）或 **应用并启动**（同步后启动游戏）。  
4. 可选：打开 **Mod 浏览** → **刷新目录** → 对条目 **加入本地库**，再回方案里勾选启用。

数据默认在 `%AppData%\MechabellumModManager\`（本地库与配置；安装包**不会**自带第三方 Mod）。

高风险 Mod 启用前会要求确认。状态栏与同步日志可查看是否与游戏目录一致。

设置里点 **检查更新**：读取 GitHub Release 上的 `latest.json`，**不会**静默自动安装。

---

## 4. MelonLoader (中文)

请在**安装包（Setup）**中勾选安装。管理器内**不再**提供一键安装 MelonLoader。  

缺少 Loader 时：重新运行 Setup 并勾选 MelonLoader，或按官方说明手动安装到游戏目录。

---

## 5. 风险提示 (中文)

仅建议 QoL / 界面类 Mod；修改战斗逻辑可能导致 Data Error / 处罚等，风险自负。  
详细免责声明见应用内「关于与声明」。

---

## 1. Requirements (English)

- Windows 10 / 11 **x64**
- Steam *Mechabellum*
- Prefer the **Setup** from [Releases](https://github.com/llxlzx/MechabellumModManager/releases/latest) (run as Administrator)

---

## 2. Install (recommended) (English)

1. Run `MechabellumModManager_Setup_v*.exe` as **Administrator**.  
2. Choose **where to install the manager**.  
3. Choose the **game folder** (must contain `Mechabellum.exe` and `GameAssembly.dll`; Steam path can be auto-detected).  
4. Components (recommended defaults):
   - **.NET 8 Desktop Runtime** — manager  
   - **.NET 6 Desktop Runtime** — common MelonLoader dependency  
   - **MelonLoader** — if not already installed in the game (skipped when present)  
5. Launch from the Start Menu when finished.

If the matching Runtime is already installed, Setup skips it.

Offline builds: place official Runtime / MelonLoader zips into `installer/redist/` before compiling Setup.

---

## 3. Daily use (English)

1. **Import** mods (DLL / Zip / folder) or **Import from game**.  
2. Select a **profile**, tick the mods to enable.  
3. **Apply profile** or **Apply and launch**.  
4. Optional: **Browse mods** → **Refresh catalog** → **Add to library**, then enable in a profile.

Data defaults to `%AppData%\MechabellumModManager\` (empty library; Setup does **not** ship third-party mods).

High-risk mods require confirmation. Use the status bar and sync log to verify deployment.

**Check for updates** reads `latest.json` from GitHub Releases — **no** silent auto-install.

---

## 4. MelonLoader (English)

Install MelonLoader **only via the Setup installer**. The manager no longer offers one-click Melon install.

If Loader is missing: re-run Setup and tick MelonLoader, or install manually per official docs into the game folder.

---

## 5. Risk notice (English)

Prefer QoL / UI mods; combat-logic changes may cause Data Error / penalties — use at your own risk.  
Full disclaimer: in-app **About / Credits**.
