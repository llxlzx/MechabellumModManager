# UI 美化（同源图标 + 排版）设计

日期：2026-09-03  
状态：已批准

## 目标

- 任务栏 / exe 使用从 `Mechabellum.exe` 提取的同源图标
- 主窗口排版与工业风视觉层次加强
- **不**将网上游戏宣传图打入仓库；背景以 XAML 氛围层完成

## 非目标

- 不改业务逻辑、命令绑定、风险流程
- 不引入花哨动效 / 紫色 SaaS 风
- 不打包第三方版权宣传图

## 实现要点

1. `Assets/app.ico`：多尺寸提取自游戏 exe；`ApplicationIcon` + `Window.Icon`
2. 顶栏：图标 + 品牌；操作按钮分组
3. 背景：深色渐变 + 轻网格/暗角；保持正文对比度
4. 主 CTA（应用并启动）继续用 Accent 样式突出
