# Release artifacts

- **Setup** (`v1.0.0/MechabellumModManager_Setup_v1.0.0.exe`): full installer with MelonLoader offline package embedded.
- **Portable** (`v1.0.0/portable/`): run `MechabellumModManager.exe` directly; requires **.NET 8 Desktop** Runtime.

See `latest.json` for the update-checker metadata.
