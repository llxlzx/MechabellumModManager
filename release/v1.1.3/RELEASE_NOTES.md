# v1.1.3（预览）— 双服 / Phase0 / 空壳硬门 / 向导零点击 / 单实例 / 彻底清理

## 本包增量（相对此前同号 1.1.3 预览包）

- **空壳仓 / 假 Ready 硬门**：激活仓缺游戏根时自动降级；ACF/快照失败不清 settle；settle 中可切完整仓 / 解除双服。
- **Phase0 Steam 生命周期**：不自动 `steam://open`；退出仅客户端；忽略 `steamservice`。
- **向导 WaitingDownloadB 零点击**：就绪后自动 ArchiveB，无「是/否」。
- **单实例**：第二开 UI 提示已在运行后退出。
- **彻底清理（纯净游戏）**：卸载与管理器内共用白名单清理 Melon/Mods/AppData；正式服不完整则中止不删仓；不卸 .NET、不删游戏本体。
- **卸载清理修复**：Steam 开着不再整段中止；仅游戏进程会阻断 Melon 删除；卸载步骤在删 `{app}` 之前执行清理；多仓根都会扫 Melon。
- **向导等待文案**：去掉「无需点是」；说明「确定」只关提示；下完但 Steam 仍开着时任务条提示「请退出 Steam」。
- **结算弹窗文案**：说明「确定」只关提示；结算需回设置页点金色「…继续」。
- **ACF 快照硬门禁**：未对齐/同 buildid 的假快照不存不恢复；空壳仓禁止切服；sanitize 会清同 buildid 毒快照。
- 构建见 `latest.json` 的 `build` 字段（本包 `98e7906`）。

## 修复要点（本号累计）

1. 解除双服：测服态先切正式服再解除。
2. Busy 关窗：确认后取消任务并退出。
3. Melon 程序集生成：超时 + 心跳；可取消。
4. Steam 结算门禁：518 / HardFault / 快照失败保持结算。
5. 诊断包：summary.md + timeline.jsonl。
6. Phase0 / 空壳假 Ready / 向导零点击 / 单实例 / 彻底清理：见上方增量。

## UX（解除双服确认）

- 「是否删除另一服」默认「否」时，**否**为金色主按钮。
- 解除/保留文案一次说清两种选择后果。
